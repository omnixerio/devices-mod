modLoader = "javafml" #mandatory
loaderVersion = "${neoforge_loader_version_range}" #mandatory
license = "${license}" # Review your options at https://choosealicense.com/.
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/" #optional
[[mods]] #mandatory
modId = "devices" #mandatory
version = "26.1.0.1" #mandatory
displayName = "Omnixerio Devices Mod" #mandatory
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional, see https://docs.neoforged.net/docs/misc/updatechecker/
displayURL="https://ultreon.dev/" #optional, displayed in the mod UI
logoFile = "omnixerio_devices.png" #optional
credits = "${credits}" #optional
authors = "Omnixerio Studios" #optional
description = '''A mod that adds working electronic devices''' #mandatory Supports multiline text
[[mixins]]
config = "devices.mixins.json"
[[mixins]]
config = "devices.neoforge.mixins.json"
[[dependencies.devices]] #optional
modId = "neoforge" #mandatory
type = "required" #mandatory Can be one of "required", "optional", "incompatible" or "discouraged"
versionRange = "[26.1,26.2)" #mandatory
ordering = "NONE" # The order that this dependency should load in relation to your mod, required to be either 'BEFORE' or 'AFTER' if the dependency is not mandatory
side = "BOTH" # Side this dependency is applied on - 'BOTH', 'CLIENT' or 'SERVER'
[[dependencies.devices]]
modId = "minecraft"
type = "required" #mandatory Can be one of "required", "optional", "incompatible" or "discouraged"
versionRange = "[26.1,26.2)"
ordering = "NONE"
side = "BOTH"

# Features are specific properties of the game environment, that you may want to declare you require. This example declares
# that your mod requires GL version 3.2 or higher. Other features will be added. They are side aware so declaring this won't
# stop your mod loading on the server for example.
#[features.${mod_id}]
#openGLVersion="[3.2,)"
